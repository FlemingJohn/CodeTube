export type Chapter = {
  id: string;
  timestamp: string;
  title: string;
  summary: string;
  code: string;
  // A placeholder for the actual transcript for AI summary generation
  transcript: string;
};
